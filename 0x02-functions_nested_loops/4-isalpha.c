#include "main.h"
/**
 * _isalpha - check if alphabet
 * @c: char to check
 * Return: 1(is alphabet) 0(not alphabet)
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (x >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	return (0);
}
